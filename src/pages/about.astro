---
import AboutHero from '../components/about-hero.astro';
import PageContent from '../components/page-content.astro';
import CtaSection from '../components/cta-section.astro';
import ImageLightbox from '../components/image-lightbox.astro';
import { IMAGE_BASE_PATH } from '../utils/constants';

// About section content - to be included in index page
---

<AboutHero />

<PageContent>
    <section class="about-intro py-5">
        <div class="container">
            <div class="row g-4 align-items-center">
                <div class="col-lg-6">
                    <h2 class="mb-4">O našem studiu</h2>
                    <p class="description-muted">Nabízíme Vám širokou nabídku služeb péče o vzhled a celé tělo. Kombinujeme profesionální kosmetické techniky, moderní přístrojové ošetření a individuální přístup, díky kterému se budete cítit sebevědomě, odpočatě a krásně.</p>
                    <p class="description-muted">Náš salon  se nachází na dobře přístupném místě v ulici Černokostelecká u zastávky autobusu (Sokolovna), s možností bezplatného parkování v okolí.</p>
                    <p class="description-muted">Těší se na Vás majitelka Eva a tým PB.</p>
                </div>
                <div class="col-lg-6">
                    <img src={`${IMAGE_BASE_PATH}/salon/uvod.jpg`} alt="Interiér studia" class="studio-intro-image" />
                </div>
            </div>
        </div>
    </section>

    <section class="studio-highlights py-5 bg-light">
        <div class="container">
            <h2 class="section-title">Co nás dělá výjimečnými</h2>
            <div class="row g-4">
                <div class="col-sm-6 col-lg-6">
                    <div class="feature-card text-center p-4 h-100">
                        <div class="feature-icon mb-3">👤</div>
                        <h3 class="h5 mb-3">Personalizovaný přístup</h3>
                        <p class="description-muted mb-0">Každé ošetření ošetření je konzultováno a přizpůsobeno Vašim preferencím a osobnímu stylu. Vybereme nejvhodnější způsob ošetření s přihlédnutím k Vašim rysům a typu pleti.</p>
                    </div>
                </div>
                <div class="col-sm-6 col-lg-6">
                    <div class="feature-card text-center p-4 h-100">
                        <div class="feature-icon mb-3">✨</div>
                        <h3 class="h5 mb-3">Osvědčené produkty</h3>
                        <p class="description-muted mb-0">Používáme pouze osvědčené produkty renomovaných firem: SynCare, ItalWax, Podopharm, Spirularin...</p>
                    </div>
                </div>
                <div class="col-sm-6 col-lg-6">
                    <div class="feature-card text-center p-4 h-100">
                        <div class="feature-icon mb-3">💄</div>
                        <h3 class="h5 mb-3">Držíme krok s trendy</h3>
                        <p class="description-muted mb-0">Neustále se seznamujeme inovacemi v oboru a průběžně si doplňujeme vzdělání.</p>
                    </div>
                </div>
                <div class="col-sm-6 col-lg-6">
                    <div class="feature-card text-center p-4 h-100">
                        <div class="feature-icon mb-3">🛡️</div>
                        <h3 class="h5 mb-3">Bezpečnost</h3>
                        <p class="description-muted mb-0">Dodržujeme nejpřísnější hygienické standardy pro Váš klid a bezpečnost.</p>
                    </div>
                </div>

            </div>
        </div>
    </section>

    <section class="studio-gallery-preview py-5">
        <div class="container">
            <h2 class="section-title">Naše studio</h2>
            <div class="row g-4">
                <div class="col-md-6">
                    <div class="studio-image clickable-image" data-src={`${IMAGE_BASE_PATH}/salon/s0.jpg`}>
                        <img src={`${IMAGE_BASE_PATH}/salon/s0.jpg`} alt="Salon - recepce" class="img-fluid rounded" />
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="studio-image clickable-image" data-src={`${IMAGE_BASE_PATH}/salon/s3.jpg`}>
                        <img src={`${IMAGE_BASE_PATH}/salon/s3.jpg`} alt="Salon - ošetřovací místnost" class="img-fluid rounded" />
                    </div>
                </div>
            </div>
        </div>
    </section>

    <ImageLightbox />

    <CtaSection 
        title="Jste připraveni zažít rozdíl?"
        text="Kontaktujte nás a dozvězte se více o tom, jak trvalá krása může zjednodušit vaši rutinu."
    />
</PageContent>

<style>
    .placeholder-image {
        background: linear-gradient(135deg, var(--color-bg-light) 0%, #E0E0E0 100%);
        border-radius: 8px;
        aspect-ratio: 4/3;
        display: flex;
        align-items: center;
        justify-content: center;
        color: var(--color-text-light);
        font-weight: 600;
        min-height: 250px;
        max-width: 100%;
    }

    .studio-intro-image {
        width: 100%;
        height: auto;
        border-radius: 8px;
        object-fit: cover;
        aspect-ratio: 4/3;
    }

    @media (max-width: 576px) {
        .studio-intro-image {
            aspect-ratio: 16/9;
        }

        .about-intro .placeholder-image {
            min-height: 200px;
            aspect-ratio: 16/9;
        }
    }

    .about-intro h2 {
        color: var(--color-secondary);
        font-size: 2rem;
        font-weight: 600;
    }

    .feature-card {
        background-color: white;
        border-radius: 8px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        transition: transform var(--transition-speed), box-shadow var(--transition-speed);
    }

    .feature-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 4px 16px rgba(0, 0, 0, 0.15);
    }

    .feature-icon {
        font-size: 3rem;
    }

    .studio-image {
        cursor: pointer;
        transition: transform 0.2s ease, opacity 0.2s ease;
    }

    .studio-image:hover {
        transform: scale(1.02);
        opacity: 0.9;
    }

    .studio-image img {
        width: 100%;
        height: auto;
        object-fit: cover;
    }

    .studio-image .placeholder-image {
        aspect-ratio: 1;
        min-height: 200px;
    }

    .approach-item {
        background-color: white;
        border-radius: 8px;
        border-left: 4px solid var(--color-primary);
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        transition: transform var(--transition-speed);
    }

    .approach-item:hover {
        transform: translateX(5px);
    }
</style>
